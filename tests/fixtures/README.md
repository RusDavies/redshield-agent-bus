# Test Fixtures

Verifier fixtures should live under `tests/fixtures/agent_bus/` as described in `docs/architecture/FIXTURE_AUDIT_STORAGE.md`.

Only curated test fixtures and deterministic expected outputs belong here. Scratch verifier output belongs under `tmp/`, which is ignored by git.
