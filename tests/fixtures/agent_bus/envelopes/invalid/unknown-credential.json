{
  "extends": "../valid/valid-notify-minimal.json",
  "case_id": "unknown-credential",
  "description": "Rejected message with an unknown credential id.",
  "expect": {
    "valid": false,
    "errors": ["credential_unknown"]
  },
  "envelope": {
    "message_id": "msg_unknown_credential",
    "correlation_id": "corr_unknown_credential",
    "idempotency_key": "idem_unknown_credential",
    "auth": {
      "credential_id": "credential_unknown"
    }
  }
}
